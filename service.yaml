# service.yaml
apiVersion: v1
kind: Service

metadata:
  name: app-udbl-v1-service
  labels:
    app: app-udbl-v1

spec:
  selector:
    app: app-udbl-v1

  ports:
  - protocol: TCP
    port: 80
    targetPort: 80

  type: ClusterIP